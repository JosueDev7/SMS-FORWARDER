export const STORAGE_KEYS = {
  RULES: '@sms_forwarder/rules',
  CONFIG: '@sms_forwarder/config',
  EVENTS: '@sms_forwarder/events',
  MESSAGES: '@sms_forwarder/messages',
  THEME: '@sms_forwarder/theme',
} as const;
